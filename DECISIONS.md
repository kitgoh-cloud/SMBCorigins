# DECISIONS

Append-only log of cross-GSD decisions for SMBC Origin. Format: `YYYY-MM-DD · area · decision — agreed: kit + evan, YYYY-MM-DD`.

This file is canonical (per D-20). `CLAUDE.md` summarizes the active locks; on conflict, DECISIONS.md wins. Do not edit existing lines — only append new ones below.

2026-04-25 · stack · D-01: Next.js 16.2 LTS — current stable as of Apr 2026; 14 EOL Apr 30; 15 supported but 16 is the Vercel-recommended LTS with Turbopack stable, React Compiler stable, and AI-agent-friendly tooling that fits the Claude Code build flow. — agreed: kit + evan, 2026-04-25
2026-04-25 · stack · D-02: Node 24 LTS — Active LTS through Apr 2028; Node 20 EOL Apr 30; Node 22 in maintenance. Pin via `.nvmrc` and `engines.node` in `package.json` so Kit and Evan run identical versions. — agreed: kit + evan, 2026-04-25
2026-04-25 · stack · D-03: Tailwind v4 — CSS-first config (`@theme`) aligns with the design-tokens-as-CSS-variables approach in CLAUDE.md and `docs/ORIGIN_DESIGN.md`. — agreed: kit + evan, 2026-04-25
2026-04-25 · stack · D-04: Supabase hosted dev project — lets Vercel previews flip `NEXT_PUBLIC_USE_MOCK=false` against a real backend; Kit avoids local Docker. — agreed: kit + evan, 2026-04-25
2026-04-25 · stack · D-05: TypeScript `strict: true` + `noUncheckedIndexedAccess: true` — catches `T | undefined` on indexed access, critical for `data/seed.ts` lookups; intentionally not adopting `exactOptionalPropertyTypes` (too much boilerplate for a prototype). — agreed: kit + evan, 2026-04-25
2026-04-25 · stack · D-06: Package manager = npm — standard, zero-setup, fewest surprises on clone. — agreed: kit + evan, 2026-04-25
2026-04-25 · stack · D-07: Linter/formatter = ESLint flat config + Prettier — industry-standard plugin coverage (a11y, React hooks). — agreed: kit + evan, 2026-04-25
2026-04-25 · stack · D-08: Branch protection on `main` — PR required + status checks. CI typecheck + lint + Vercel preview must pass. No required reviewers (small team — trust + daily-merge cadence over enforced review). — agreed: kit + evan, 2026-04-25
2026-04-25 · scaffolding · D-09: Kit alone runs `create-next-app` and pushes the initial scaffold; Evan reviews. Preserves day-1 Vercel URL target. — agreed: kit + evan, 2026-04-25
2026-04-25 · scaffolding · D-10: Initial directory layout = root-level `app/`, `components/`, `lib/`, `types/`, `data/`. Matches REQUIREMENTS.md paths verbatim (BOUND-01..04, SCAFF-06). No `src/` wrapper. — agreed: kit + evan, 2026-04-25
2026-04-25 · scaffolding · D-11: `types/origin.ts` at repo root — single source of truth, max visibility, matches BOUND-01 path exactly. — agreed: kit + evan, 2026-04-25
2026-04-25 · scaffolding · D-12: Initial scaffold ships as one PR on branch `kit/scaffold` — reviewed and merged by Evan. — agreed: kit + evan, 2026-04-25
2026-04-25 · cross-gsd · D-13: `types/origin.ts` is co-owned with cross-review required — encoded in `.github/CODEOWNERS` so GitHub auto-requests the other person on every touching PR. Toolchain-independent enforcement. — agreed: kit + evan, 2026-04-25
2026-04-25 · cross-gsd · D-14: Contract changes are announced via Slack ping + PR link in a shared channel. Lightweight, async, no polling. — agreed: kit + evan, 2026-04-25
2026-04-25 · cross-gsd · D-15: Conflict resolution = first-PR-wins; the second person rebases on main, resolves, gets re-review. Standard git flow, no special tooling. — agreed: kit + evan, 2026-04-25
2026-04-25 · cross-gsd · D-16: `lib/api.ts` is split into `lib/api.mock.ts` (Kit) + `lib/api.real.ts` (Evan); `lib/api.ts` is the typed export-with-switch on `NEXT_PUBLIC_USE_MOCK`. Each side owns its file at `CODEOWNERS` level; the top-level switch file is co-owned. — agreed: kit + evan, 2026-04-25
2026-04-25 · closure · D-17: Closure ships as a single PR on branch `kit/phase-1-alignment` containing all four artifacts: updated `CLAUDE.md`, new `DECISIONS.md`, new `CONTRACT.md`, new `.github/CODEOWNERS`. Evan reviews and merges. — agreed: kit + evan, 2026-04-25
2026-04-25 · closure · D-18: Evidence of agreement = PR approval + per-row `— agreed: kit + evan, YYYY-MM-DD` signature in `DECISIONS.md`. PR approval is the global sign-off; per-row signatures provide an audit trail in case scope-of-approval is ever disputed. — agreed: kit + evan, 2026-04-25
2026-04-25 · closure · D-19: Fallback if Evan can't review in target window = soft-block. Kit may begin Phase 2 work on `kit/scaffold` using these locks but cannot merge to main until the Phase 1 PR closes. Preserves the day-1 deploy schedule without front-running Evan's sign-off. — agreed: kit + evan, 2026-04-25
2026-04-25 · closure · D-20: Doc roles: `DECISIONS.md` is canonical (append-only, dated, signed). `CLAUDE.md` has a "Stack contract" + "Scaffolding ownership" section that summarizes the active locks for session-start reading. On conflict, DECISIONS.md wins. — agreed: kit + evan, 2026-04-25
2026-04-25 · closure · D-21: Evan does NOT use GSD. All Phase 1 artifacts are tool-agnostic and live at repo root. Nothing in `.planning/` is canonical for the cross-GSD contract; Evan never touches `.planning/`. — agreed: kit + evan, 2026-04-25
2026-04-25 · artifacts · D-22: `DECISIONS.md` at repo root — append-only running log. Format: `YYYY-MM-DD · area · decision — agreed: kit + evan`. Initial seed = all decisions in this CONTEXT.md (D-01..D-21). — agreed: kit + evan, 2026-04-25
2026-04-25 · artifacts · D-23: `CONTRACT.md` at repo root — boundary governance: rules for `types/origin.ts` and the `lib/api.ts` split, including co-ownership + cross-review, "first-PR-wins; second rebases", and Slack-ping-on-contract-PR convention. — agreed: kit + evan, 2026-04-25
2026-04-25 · artifacts · D-24: `.github/CODEOWNERS` — auto-requires reviewer on `types/origin.ts` (both), `lib/api.ts` (both), `lib/api.mock.ts` (Kit-only writes; Evan reviews), `lib/api.real.ts` (Evan-only writes; Kit reviews). — agreed: kit + evan, 2026-04-25
2026-04-25 · artifacts · D-25: `CLAUDE.md` mutation — add "Stack contract" section (summarizes D-01..D-08) and "Scaffolding ownership" section (summarizes D-09..D-12) under existing structure. Existing content unchanged. — agreed: kit + evan, 2026-04-25
2026-04-26 · typography · D-64: IBM Plex Mono weight bundle swapped from `['400', '700']` to `['400', '500']` in `app/layout.tsx:31`. Phase 4 UI-SPEC's mono surfaces (Eyebrow 10/500, ModeSwitcher labels 14/500, Avatar initials 12/400) need weight 500; weight 700 was unused dead-weight in the bundle. Safety pre-check `grep -rn font-bold app/ components/ lib/` returned 0 matches before the swap. Supersedes nothing — Phase 2 D-31 governs Fraunces only. — agreed: kit + evan, 2026-04-26
2026-04-26 · typography · D-65: Fraunces SOFT/WONK variation rendered via inline `style={{ fontVariationSettings: '"SOFT" 80, "WONK" 1' }}` on the "Origin" wordmark `<span>` only (Phase 4 OD-12 strategy b). Phase 2 D-31 (Fraunces wght-only `axes`) stays in force — no font-import change. Other Phase 4 Fraunces consumers (StagePill numerals) intentionally do NOT use SOFT/WONK; if a future phase needs the axes globally, that phase revisits D-31. — agreed: kit + evan, 2026-04-26
2026-04-26 · architecture · D-66: D-67 (modeForPathname route-to-mode map) is amended from a 2-arm union 'client' | 'rm' to a 3-arm union 'client' | 'rm' | 'demo'. Pathnames starting with /dev return 'demo'; TopStrip suppresses persona-context block + ModeSwitcher segments are inactive in demo mode (RESEARCH §8.2). Default still falls through to 'client' for unrecognized non-/dev paths. — agreed: kit + evan, 2026-04-26
