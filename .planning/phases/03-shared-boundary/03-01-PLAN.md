---
phase: 03-shared-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
  - .github/workflows/ci.yml
  - CLAUDE.md
autonomous: true
requirements:
  - BOUND-04
user_setup: []

must_haves:
  truths:
    - "Running `npm run test` exits 0 from a clean checkout (no test files yet -> no tests to run -> non-error)"
    - "Running `npm run test:watch` enters Vitest watch mode (sanity verified by maintainer locally)"
    - "CI workflow on next push runs three jobs: typecheck, lint, test"
    - "Pre-PR validation line in CLAUDE.md includes `npm run test`"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with vite-tsconfig-paths plugin so @/ resolves at test time"
      contains: "defineConfig"
    - path: "package.json"
      provides: "test + test:watch scripts and vitest + vite-tsconfig-paths devDeps"
      contains: "\"test\": \"vitest run\""
    - path: ".github/workflows/ci.yml"
      provides: "Third top-level `test` job parallel to typecheck and lint"
      contains: "name: test"
    - path: "CLAUDE.md"
      provides: "Updated pre-PR validation line including `npm run test`"
      contains: "npm run test &&"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json paths"
      via: "vite-tsconfig-paths plugin"
      pattern: "tsconfigPaths"
    - from: ".github/workflows/ci.yml `test` job"
      to: "package.json `test` script"
      via: "npm run test"
      pattern: "npm run test"
---

<objective>
Wire Vitest into the project as the test runner, add a third `test` CI job parallel to existing `typecheck` and `lint` (Pattern D from `02-PATTERNS.md`), and update CLAUDE.md's pre-PR validation line. This plan adds zero test files — it only stands up the runner so plans 02, 04, 05 can author co-located `*.test.ts` files that run on `npm run test`.

Purpose: Without this plan, the test files written in plans 02/04/05 cannot execute. This plan is the prerequisite for D-60's invariant tests (BOUND-03 enforcement) and D-54's idempotency tests (BOUND-04 contract).

Output: A configured Vitest install + a CI job that exits 0 today (zero tests collected = pass) and will gate plans 02/04/05 once they add test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-shared-boundary/03-CONTEXT.md
@.planning/phases/03-shared-boundary/03-PATTERNS.md
@.planning/phases/02-scaffolding/02-PATTERNS.md
@CLAUDE.md
@CONTRACT.md
@.github/CODEOWNERS
@.github/workflows/ci.yml
@package.json
@tsconfig.json

<interfaces>
<!-- Existing CI job pattern that the new `test` job MUST mirror exactly -->

From .github/workflows/ci.yml (Phase 2's `typecheck` job, lines 14-25):
```yaml
jobs:
  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'
      - run: npm ci
      - run: npm run typecheck
```

From package.json (existing scripts block, lines 8-14):
```json
"scripts": {
  "dev": "next dev --turbopack",
  "build": "next build",
  "start": "next start",
  "lint": "eslint .",
  "typecheck": "tsc --noEmit"
}
```

From tsconfig.json (existing path alias, line 22-24 — Vitest must respect this):
```json
"paths": {
  "@/*": ["./*"]
}
```

From CLAUDE.md line 106 (the literal line to modify):
```
Pre-PR validation: `npm run typecheck && npm run lint && npm run build`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Install Vitest + vite-tsconfig-paths and add npm scripts</name>
  <files>package.json, package-lock.json (auto-updated by npm)</files>
  <read_first>
    - /Users/wyekitgoh/Projects/SMBCorigins/package.json (lines 1-32 — full file is small)
    - /Users/wyekitgoh/Projects/SMBCorigins/.planning/phases/03-shared-boundary/03-PATTERNS.md (the `package.json (MODIFY)` section, lines 680-703)
    - /Users/wyekitgoh/Projects/SMBCorigins/.nvmrc (sanity-check Node version pin)
  </read_first>
  <action>
    Run from repo root: `npm install --save-dev vitest vite-tsconfig-paths`. Let npm pick the version range — do NOT hand-pick a specific major. After install, edit `package.json`:

    1. Add two scripts to the `scripts` block (insert after the existing `"typecheck": "tsc --noEmit"` line, preserving alphabetical-ish order is fine but keep `dev` first):
       - `"test": "vitest run"`
       - `"test:watch": "vitest"`

       The exact diff inside `scripts` should produce:
       ```json
       "scripts": {
         "dev": "next dev --turbopack",
         "build": "next build",
         "start": "next start",
         "lint": "eslint .",
         "typecheck": "tsc --noEmit",
         "test": "vitest run",
         "test:watch": "vitest"
       }
       ```

    2. Confirm `vitest` and `vite-tsconfig-paths` were appended to `devDependencies` by `npm install` and that `package-lock.json` updated. Do NOT manually pin versions — leave whatever range npm wrote.

    3. Do NOT add a `"vitest": {...}` config block at the top level of `package.json` — config lives in `vitest.config.ts` (next task).

    Commit message convention (per CONTEXT §"Established Patterns"): `chore(03-01): add vitest + vite-tsconfig-paths` — but DO NOT commit yet; commit comes after all four tasks in this plan land in working tree.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (p.scripts.test !== 'vitest run') process.exit(1); if (p.scripts['test:watch'] !== 'vitest') process.exit(2); if (!p.devDependencies.vitest) process.exit(3); if (!p.devDependencies['vite-tsconfig-paths']) process.exit(4); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts.test` equals exactly `"vitest run"`
    - `package.json` `scripts["test:watch"]` equals exactly `"vitest"`
    - `package.json` `devDependencies.vitest` is present (any version range)
    - `package.json` `devDependencies["vite-tsconfig-paths"]` is present (any version range)
    - `package-lock.json` has been updated (file mtime changed; `node_modules/vitest/` exists)
    - `grep -c '"jest"' package.json` returns 0 (we did not accidentally add jest config)
  </acceptance_criteria>
  <done>
    `npm run test --silent` (no test files yet) exits 0 with output containing "No test files found" — this is correct/expected; vitest treats zero matches as success in CI.
  </done>
</task>

<task type="auto">
  <name>Task 1.2: Create vitest.config.ts (minimal, node env, @/ alias)</name>
  <files>vitest.config.ts</files>
  <read_first>
    - /Users/wyekitgoh/Projects/SMBCorigins/.planning/phases/03-shared-boundary/03-PATTERNS.md (the `vitest.config.ts` section, lines 340-366)
    - /Users/wyekitgoh/Projects/SMBCorigins/tsconfig.json (lines 22-24 — confirm `@/*` path alias still maps to `./*`)
    - /Users/wyekitgoh/Projects/SMBCorigins/next.config.ts (idiom only — TypeScript-config-as-module)
  </read_first>
  <action>
    Create `vitest.config.ts` at repo root with EXACTLY this content (no extra options, no `coverage:`, no `setupFiles:`):

    ```typescript
    import { defineConfig } from 'vitest/config'
    import tsconfigPaths from 'vite-tsconfig-paths'

    export default defineConfig({
      plugins: [tsconfigPaths()],
      test: {
        environment: 'node',
        globals: false,
        include: ['**/*.test.ts'],
      },
    })
    ```

    Why each option (per 03-PATTERNS.md "vitest.config.ts" section):
    - `tsconfigPaths()`: Vitest does NOT natively read `tsconfig.json` `paths`; without this plugin `import { ... } from '@/types/origin'` breaks at test runtime even though typecheck passes.
    - `environment: 'node'`: Phase 3 has zero React component tests (deferred per Phase 2). No jsdom needed.
    - `globals: false`: Forces every test file to do `import { describe, it, expect } from 'vitest'` — explicit dependency in code review, dovetails with strict TS.
    - `include: ['**/*.test.ts']`: Co-located test pattern (Pattern I from 03-PATTERNS.md).

    Do NOT add `node_modules/` to a custom `exclude` — Vitest's default exclude already covers it.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('vitest.config.ts','utf8'); if (!c.includes('tsconfigPaths()')) process.exit(1); if (!c.includes(\"environment: 'node'\")) process.exit(2); if (!c.includes('globals: false')) process.exit(3); if (!c.includes(\"include: ['**/*.test.ts']\")) process.exit(4); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/wyekitgoh/Projects/SMBCorigins/vitest.config.ts` exists
    - First non-comment line is `import { defineConfig } from 'vitest/config'`
    - Contains exact substring `tsconfigPaths()`
    - Contains exact substring `environment: 'node'`
    - Contains exact substring `globals: false`
    - Contains exact substring `include: ['**/*.test.ts']`
    - File is < 20 lines (the spec is minimal — no creep)
    - `npx tsc --noEmit` still passes (the config file participates in `tsconfig.json` include `**/*.ts`)
  </acceptance_criteria>
  <done>
    Running `npx vitest run --reporter=verbose` from the repo root exits 0 and prints "No test files found, exiting with code 0" (zero tests collected = success). Confirms config parses without error.
  </done>
</task>

<task type="auto">
  <name>Task 1.3: Add `test` job to .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /Users/wyekitgoh/Projects/SMBCorigins/.github/workflows/ci.yml (lines 1-37 — full file, study `typecheck` and `lint` jobs as the analog)
    - /Users/wyekitgoh/Projects/SMBCorigins/.planning/phases/03-shared-boundary/03-PATTERNS.md (the `.github/workflows/ci.yml (MODIFY)` section, lines 625-676)
    - /Users/wyekitgoh/Projects/SMBCorigins/.planning/phases/02-scaffolding/02-PATTERNS.md (search for "Pattern D" — the third-job idiom)
  </read_first>
  <action>
    Append a third top-level job named `test` to `.github/workflows/ci.yml`. It MUST be a sibling of `typecheck` and `lint` (NOT a matrix step, NOT a step within `lint`). Same indentation as `typecheck:` (two spaces from column 0). After the modification, the `jobs:` block should contain three top-level jobs in this order: typecheck, lint, test.

    Append AFTER line 37 (`      - run: npm run lint`) so the file ends with:

    ```yaml
      test:
        name: test
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version-file: '.nvmrc'
              cache: 'npm'
          - run: npm ci
          - run: npm run test
    ```

    DO NOT change anything above line 37. DO NOT add a per-job `permissions:` override (workflow-level `permissions: contents: read` on line 11-12 already covers this job — `npm test` needs no elevated scope).

    The status check name will be `test` (from the `name: test` field). After the first push hits CI, the maintainer will manually add `test` as a fourth required status check via GitHub Settings -> Branches -> Edit rule (this is a UI step, not in-repo, and is documented in 03-PATTERNS.md line 676 — NOT this plan's responsibility, but flag it in the SUMMARY.md output).

    Confirm the file ends with a single trailing newline (no double-newline at EOF).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('.github/workflows/ci.yml','utf8'); const m = c.match(/^  (typecheck|lint|test):/gm) || []; if (m.length !== 3) { console.error('expected 3 top-level jobs, got', m.length, m); process.exit(1); } if (!c.includes('name: test')) process.exit(2); if (!c.includes('npm run test')) process.exit(3); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains EXACTLY three top-level job declarations matching `^  (typecheck|lint|test):` — verified by regex count
    - Contains exact substring `name: test`
    - Contains exact substring `npm run test`
    - The new `test:` block uses `actions/checkout@v4` (matches existing jobs)
    - The new `test:` block uses `actions/setup-node@v4` with `node-version-file: '.nvmrc'` (matches existing jobs)
    - `permissions: contents: read` (workflow-level, line 11-12) is unchanged
    - File parses as valid YAML: `node -e "require('js-yaml')" 2>/dev/null` is irrelevant; instead use `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` if Python is available, else accept the regex check above as sufficient
  </acceptance_criteria>
  <done>
    The `.github/workflows/ci.yml` file declares three jobs (typecheck, lint, test), all structurally similar (same checkout + setup-node + npm ci + npm run X pattern). On the next push, GitHub Actions will run all three in parallel.
  </done>
</task>

<task type="auto">
  <name>Task 1.4: Update CLAUDE.md "How to run" pre-PR validation line</name>
  <files>CLAUDE.md</files>
  <read_first>
    - /Users/wyekitgoh/Projects/SMBCorigins/CLAUDE.md (line 106 specifically, plus the surrounding "How to run" section context lines 98-109)
    - /Users/wyekitgoh/Projects/SMBCorigins/.planning/phases/03-shared-boundary/03-PATTERNS.md (the `CLAUDE.md (MODIFY)` section, lines 706-722 — note CRITICAL warning about not modifying any other section)
  </read_first>
  <action>
    Edit `CLAUDE.md` line 106 ONLY. Replace exactly this line:

    ```
    Pre-PR validation: `npm run typecheck && npm run lint && npm run build`.
    ```

    With exactly this line:

    ```
    Pre-PR validation: `npm run typecheck && npm run lint && npm run test && npm run build`.
    ```

    The order matters: `test` goes between `lint` and `build` — typecheck first (fastest, catches structural drift), lint second (catches style/best-practice violations), test third (catches behavioral regressions on data/mock changes), build last (the slowest/most-expensive check that gates the deploy).

    CRITICAL — DO NOT modify any other section of CLAUDE.md (per 03-PATTERNS.md line 720-722 + 02-PATTERNS.md line 412): "Stack contract / Scaffolding ownership / Design system / Personas are Phase 1 closure content — overwriting them is the failure mode that --no-agents-md is specifically designed to prevent." This is a single-line edit ONLY.

    No other changes to "How to run", "Stack contract", "Scaffolding ownership", "Working principles", "Personas", "Hero scenario", "The six stages", "Design system", "Reference docs", "Non-goals". Do not reflow whitespace, do not normalize markdown style, do not add a Phase 3 note.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('CLAUDE.md','utf8'); if (!c.includes('npm run typecheck && npm run lint && npm run test && npm run build')) process.exit(1); if (c.includes('npm run typecheck && npm run lint && npm run build`')) process.exit(2); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains exact substring `npm run typecheck && npm run lint && npm run test && npm run build`
    - `CLAUDE.md` does NOT contain the old substring `npm run typecheck && npm run lint && npm run build` (would mean the edit didn't take)
    - `git diff CLAUDE.md` shows exactly one line changed (one `-` and one `+`) — no incidental edits elsewhere
    - All other `## ` heading sections in CLAUDE.md (`## What we're building`, `## Who's building it`, `## Stack contract`, `## Scaffolding ownership`, `## Working principles`, `## Personas`, `## Hero scenario`, `## The six stages`, `## Design system (locked — do not deviate)`, `## Reference docs`, `## Non-goals`, `## How to run`) remain present
  </acceptance_criteria>
  <done>
    CLAUDE.md shows `test` between `lint` and `build` in the pre-PR validation line. The four-step pre-PR validation chain (typecheck -> lint -> test -> build) matches what CI now runs (typecheck + lint + test) plus build (which Vercel runs at deploy time).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> CI | New `test` CI job runs `npm run test` against PR code on GitHub Actions runners |
| GitHub Actions runner -> npm registry | `npm ci` pulls vitest + vite-tsconfig-paths from npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T (Tampering) | npm registry supply chain (vitest + vite-tsconfig-paths) | accept | Both packages are widely used (vitest >40k weekly downloads, vite-tsconfig-paths >2M). `package-lock.json` pins exact resolved versions. `npm ci` in CI uses lockfile (no resolution drift between local and CI). Severity: low for a prototype demo. |
| T-03-01-02 | E (Elevation of Privilege) | New `test` job permissions in ci.yml | mitigate | Workflow-level `permissions: contents: read` (line 11-12) covers the new job; no per-job override added. `npm run test` needs no elevated scope (no writes, no remote calls). |
| T-03-01-03 | T (Tampering) | CLAUDE.md scope-creep edit | mitigate | Task 1.4 explicitly forbids editing any other section; verification asserts `git diff` shows exactly one line changed. |
| T-03-01-04 | I (Information Disclosure) | Vitest `globals: false` choice | accept | Forces explicit imports of `describe`/`it`/`expect`, makes test dependencies visible in code review (per 03-PATTERNS.md). Not a security threat per se but reduces accidental shadow-globaling. Severity: N/A. |
</threat_model>

<verification>
Final cross-task verification (run all four):

1. `node -e "const p = require('./package.json'); console.log(p.scripts.test, p.scripts['test:watch'], !!p.devDependencies.vitest, !!p.devDependencies['vite-tsconfig-paths'])"` prints `vitest run vitest true true`.
2. `node -e "console.log(require('fs').existsSync('vitest.config.ts'))"` prints `true`.
3. `grep -c "^  test:" .github/workflows/ci.yml` returns `1` (exactly one new test job).
4. `grep -c "npm run test && npm run build" CLAUDE.md` returns `1` (the updated pre-PR line).
5. End-to-end: `npm install && npm run test` exits 0 with "No test files found" (Vitest treats zero tests as success in non-watch mode by default in v2+; if v3 changes default, add `--passWithNoTests` to the script — flag in SUMMARY).

Note: If Vitest 3.x changes the zero-test default to non-zero exit, the fix is `"test": "vitest run --passWithNoTests"`. Document this in SUMMARY.md if encountered. The downstream plans (02, 04, 05) will add real tests so this edge case is short-lived.
</verification>

<success_criteria>
- vitest + vite-tsconfig-paths are devDependencies; `package-lock.json` updated
- `npm run test` and `npm run test:watch` scripts work
- `vitest.config.ts` is minimal (Pattern I established): node env, globals: false, `**/*.test.ts` include, tsconfigPaths plugin
- CI workflow has three top-level jobs (typecheck, lint, test) all running in parallel
- CLAUDE.md pre-PR validation now runs `test` between `lint` and `build`
- `npm run typecheck` still passes (vitest.config.ts is included via tsconfig glob and parses cleanly)
- `npm run lint` still passes (eslint flat config tolerates the new TS file)
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-boundary/03-01-SUMMARY.md` documenting:
- Files created: `vitest.config.ts`
- Files modified: `package.json` (+ `package-lock.json`), `.github/workflows/ci.yml`, `CLAUDE.md`
- Vitest version pinned by npm: report the actual version range npm picked
- Note on `--passWithNoTests`: whether vitest's zero-test exit code required adjusting the script
- TODO for maintainer (not Claude): manually add `test` as a fourth required status check on `main` via GitHub Settings -> Branches -> Edit rule, AFTER the first CI run on this branch validates the job name surfaces correctly
- Patterns established: I (co-located Vitest tests with node env)
</output>
