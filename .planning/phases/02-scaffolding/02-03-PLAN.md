---
id: 02-03
phase: 02-scaffolding
plan: 03
title: "Design tokens (@theme) + four next/font fonts + root token showcase page"
type: execute
status: draft
wave: 2
depends_on: [02-01]
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
autonomous: true
requirements: [SCAFF-03, SCAFF-04, SCAFF-05]
requirements_addressed: [SCAFF-03, SCAFF-04, SCAFF-05]

must_haves:
  truths:
    - "app/globals.css contains all 18 color tokens from docs/ORIGIN_DESIGN.md §8.1 verbatim (six greens, six neutrals, three signals, three dark)"
    - "app/globals.css contains all 9 spacing tokens (4, 8, 12, 16, 24, 32, 48, 64, 96 px) and 3 radius tokens (button, card, modal)"
    - "app/globals.css has a separate `@theme inline {}` block remapping next/font CSS vars to --font-display/-body/-jp/-mono"
    - "app/globals.css has a code comment next to fresh-green tokens noting AI-only rule (D-28; enforcement deferred to Phase 4)"
    - "app/layout.tsx imports ALL FOUR fonts from next/font/google: Fraunces, Inter_Tight, Noto_Sans_JP, IBM_Plex_Mono"
    - "app/layout.tsx applies all four `.variable` classNames to <html> (D-33)"
    - "Fraunces uses axes: ['wght'] only (D-31 — no opsz/SOFT/WONK)"
    - "Noto Sans JP uses subsets: ['latin'] only (D-32 — JP via unicode-range autoload)"
    - "IBM Plex Mono uses weight: ['400', '700'] (NOT a variable font on Google Fonts)"
    - "app/page.tsx renders all 18 color swatches, each labeled with token name AND hex value"
    - "app/page.tsx renders four font sample paragraphs, each font's name set in itself, including a Japanese sample line in font-jp"
    - "app/page.tsx renders an IBM Plex Mono ID/timestamp sample"
    - "All three pages use bg-paper text-trad-green (D-41 minimal layout)"
    - "`npm run dev` boots and serves http://localhost:3000 with HTTP 200"
    - "`npm run build` succeeds"
    - "`npm run typecheck` and `npm run lint` exit 0"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind v4 @theme tokens (colors + spacing + radius) AND @theme inline font remap, generating utilities like bg-trad-green, font-display, p-4, rounded-card"
      contains: "@theme {"
      contains_alt: "@theme inline {"
    - path: "app/layout.tsx"
      provides: "RootLayout with four fonts wired and metadata"
      contains: "next/font/google"
    - path: "app/page.tsx"
      provides: "Token showcase rendering 18 color swatches + 4 font samples (D-41 verification surface)"
      contains: "Fraunces"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "next/font CSS vars (--font-fraunces etc) → @theme inline remap"
      pattern: "var\\(--font-(fraunces|inter-tight|noto-sans-jp|ibm-plex-mono)\\)"
    - from: "app/page.tsx"
      to: "app/globals.css tokens"
      via: "Tailwind utilities bg-{color}, font-{slot}"
      pattern: "(bg|text|font)-(trad-green|fresh-green|paper|ink|signal|display|body|jp|mono)"
    - from: "@theme inline {} block"
      to: "next/font .variable className on <html>"
      via: "Required `inline` keyword to avoid cascade scoping bugs (RESEARCH §Landmines #3)"
      pattern: "@theme inline"
---

<objective>
Land the design system: port all token literals from `docs/ORIGIN_DESIGN.md §8` into Tailwind v4's `@theme {}` block in `app/globals.css` (D-26, D-27, D-29), wire all four Google Fonts via `next/font/google` in `app/layout.tsx` with the canonical `--font-{name}` → `@theme inline` → utility chain (D-30..D-33), then build the root `/` token showcase page (D-41) that renders all 18 color swatches with hex labels and four font sample paragraphs (each font's name set in itself, plus a Japanese sample line and an IBM Plex Mono ID/timestamp sample).

Purpose: This single plan covers SCAFF-03 (Tailwind utilities apply), SCAFF-04 (design tokens from ORIGIN_DESIGN.md ported as CSS custom properties), and SCAFF-05 (four fonts visibly render). The root page is the verification surface — opening one URL post-deploy lets a reviewer confirm both palette and font requirements simultaneously (per RESEARCH §"Validation Architecture" lines 740–750: "reviewing the page IS the test"). Tokens land in the canonical analog template — Phases 3–8 inherit and never re-plumb.

Output: Booting Next.js app where http://localhost:3000 (and post-merge the Vercel URL) renders a labeled palette grid of all 18 SMBC tokens plus four font samples. Tailwind v4 auto-generates `bg-trad-green`, `font-display`, `font-jp`, `font-mono`, etc. utilities. CSS variables also exposed at `:root` for non-utility use cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scaffolding/02-CONTEXT.md
@.planning/phases/02-scaffolding/02-RESEARCH.md
@.planning/phases/02-scaffolding/02-PATTERNS.md
@.planning/phases/02-scaffolding/02-VALIDATION.md
@.planning/phases/02-scaffolding/02-01-PLAN.md
@.planning/phases/02-scaffolding/02-02-PLAN.md
@docs/ORIGIN_DESIGN.md
@DECISIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port full design-token set to app/globals.css with @theme + @theme inline blocks</name>
  <files>app/globals.css</files>
  <read_first>
    - docs/ORIGIN_DESIGN.md §8 (lines 363–410 — ALL token names + hex values verbatim source of truth per D-27)
    - DECISIONS.md (D-26 Tailwind v4 @theme, D-27 full set verbatim, D-28 Fresh Green AI-only deferred to Phase 4 with code comment, D-29 single globals.css)
    - .planning/phases/02-scaffolding/02-RESEARCH.md (§4 "app/globals.css — token plumbing" lines 261–349, §"Landmines" #3 next/font CSS vars not resolving)
    - .planning/phases/02-scaffolding/02-PATTERNS.md (§"app/globals.css")
    - app/globals.css (current state — overwritten by create-next-app in 02-01 with default Geist setup)
  </read_first>
  <action>
    Replace the entire content of `app/globals.css` with the structure below. **Use the Write tool** (this is a full rewrite of the create-next-app default file). Source of truth for every hex value is `docs/ORIGIN_DESIGN.md §8.1, §8.3, §8.4` (D-27 mandates verbatim porting).

    The file has FIVE sections in this exact order:
    1. File header comment citing ORIGIN_DESIGN.md §8 as source of truth (D-27)
    2. `@import "tailwindcss";` — Tailwind v4 entry point (D-29 — single CSS file, no `styles/tokens.css` split)
    3. `@theme {}` block with literal tokens: colors, spacing, radius
    4. `@theme inline {}` block with font CSS-var remaps (RESEARCH §"Landmines" #3 — `inline` is required)
    5. Minimal `body` reset

    Write EXACTLY this content (verbatim from RESEARCH §4 lines 263–334, with hex values verified against ORIGIN_DESIGN.md §8.1):

    ```css
    /* app/globals.css — design tokens for SMBC Origin */
    /* Source for all token values: docs/ORIGIN_DESIGN.md §8 (D-27) */
    /* Plumbing pattern: @theme generates utilities + CSS vars; @theme inline wires next/font */

    @import "tailwindcss";

    @theme {
      /* ====== Brand greens — primary SMBC palette ====== */
      --color-trad-green: #004832;       /* Primary dark, nav, headers */
      --color-trad-green-deep: #00301F;  /* Hover, depth */
      --color-trad-green-soft: #1A5F48;  /* Hover lighter */

      /* Fresh Green is RESERVED FOR AI OUTPUTS ONLY (D-28; enforcement in Phase 4 / SHELL-05).
         See CLAUDE.md "Design system" — never use on primary buttons or generic accents. */
      --color-fresh-green: #BFD730;
      --color-fresh-green-mute: #D4E566;
      --color-fresh-green-glow: #BFD73022;

      /* ====== Neutrals ====== */
      --color-ink: #0A1410;
      --color-ink-soft: #3C4540;
      --color-ink-muted: #7A827D;
      --color-paper: #FAFBF7;
      --color-paper-deep: #F3F5EE;
      --color-mist: #E8EDE4;

      /* ====== Signals ====== */
      --color-signal-amber: #E8A317;
      --color-signal-red: #C73E1D;
      --color-signal-info: #2A6F97;

      /* ====== Dark mode (RM cockpit option — tokens only, not wired in v1) ====== */
      --color-dark-bg: #0B1F17;
      --color-dark-surface: #112820;
      --color-dark-text: #E8EDE4;

      /* ====== Spacing — 8px base scale (ORIGIN_DESIGN.md §8.3) ====== */
      --spacing-1: 4px;
      --spacing-2: 8px;
      --spacing-3: 12px;
      --spacing-4: 16px;
      --spacing-6: 24px;
      --spacing-8: 32px;
      --spacing-12: 48px;
      --spacing-16: 64px;
      --spacing-24: 96px;

      /* ====== Radius (ORIGIN_DESIGN.md §8.4) ====== */
      --radius-button: 6px;   /* Buttons / inputs */
      --radius-card: 12px;    /* Cards */
      --radius-modal: 16px;   /* Modals */
      /* Avatars use `rounded-full` (built-in) */
    }

    @theme inline {
      /* Remap next/font CSS variables to Tailwind font namespace.
         The `inline` keyword inlines the var() reference, avoiding cascade scoping bugs
         when next/font writes the var on <html> and Tailwind utilities resolve it elsewhere.
         (RESEARCH §"Landmines" #3 — without `inline`, the indirection chain breaks in
         Shadow DOM and certain CSS isolations.) */
      --font-display: var(--font-fraunces);     /* Headings, display, stat numerals */
      --font-body: var(--font-inter-tight);     /* UI body */
      --font-jp: var(--font-noto-sans-jp);      /* Japanese characters */
      --font-mono: var(--font-ibm-plex-mono);   /* Data, IDs, timestamps */
    }

    /* Base resets — minimal. App shell (Phase 4) will add more. */
    body {
      background-color: var(--color-paper);
      color: var(--color-ink);
      font-family: var(--font-body), system-ui, sans-serif;
    }
    ```

    **Critical:** the hex values MUST match docs/ORIGIN_DESIGN.md §8.1 EXACTLY. The Source of Truth file lists them at lines 367–391 with the bare names (`--trad-green`, etc.); Phase 2 prefixes them with `--color-` per Tailwind v4 namespace convention so they auto-generate `bg-{name}` utilities (RESEARCH §4 line 343). DO NOT prefix with anything else (e.g., not `--smbc-trad-green`).

    **DO NOT add** other tokens (motion durations, shadows) — those are Phase 4+ scope.
    **DO NOT remove** any hex value from the §8.1 set — D-27 mandates "verbatim full set."
  </action>
  <verify>
    <automated>
      cd /Users/wyekitgoh/Projects/SMBCorigins &&
      grep -q '@import "tailwindcss";' app/globals.css &&
      grep -q '^@theme {' app/globals.css &&
      grep -q '^@theme inline {' app/globals.css &&
      grep -q '\-\-color-trad-green: #004832;' app/globals.css &&
      grep -q '\-\-color-trad-green-deep: #00301F;' app/globals.css &&
      grep -q '\-\-color-trad-green-soft: #1A5F48;' app/globals.css &&
      grep -q '\-\-color-fresh-green: #BFD730;' app/globals.css &&
      grep -q '\-\-color-fresh-green-mute: #D4E566;' app/globals.css &&
      grep -q '\-\-color-fresh-green-glow: #BFD73022;' app/globals.css &&
      grep -q '\-\-color-ink: #0A1410;' app/globals.css &&
      grep -q '\-\-color-paper: #FAFBF7;' app/globals.css &&
      grep -q '\-\-color-mist: #E8EDE4;' app/globals.css &&
      grep -q '\-\-color-signal-amber: #E8A317;' app/globals.css &&
      grep -q '\-\-color-signal-red: #C73E1D;' app/globals.css &&
      grep -q '\-\-color-signal-info: #2A6F97;' app/globals.css &&
      grep -q '\-\-color-dark-bg: #0B1F17;' app/globals.css &&
      grep -q '\-\-color-dark-surface: #112820;' app/globals.css &&
      grep -q '\-\-color-dark-text: #E8EDE4;' app/globals.css &&
      grep -q '\-\-spacing-1: 4px;' app/globals.css &&
      grep -q '\-\-spacing-24: 96px;' app/globals.css &&
      grep -q '\-\-radius-button: 6px;' app/globals.css &&
      grep -q '\-\-radius-card: 12px;' app/globals.css &&
      grep -q '\-\-radius-modal: 16px;' app/globals.css &&
      grep -q '\-\-font-display: var(\-\-font-fraunces)' app/globals.css &&
      grep -q '\-\-font-body: var(\-\-font-inter-tight)' app/globals.css &&
      grep -q '\-\-font-jp: var(\-\-font-noto-sans-jp)' app/globals.css &&
      grep -q '\-\-font-mono: var(\-\-font-ibm-plex-mono)' app/globals.css &&
      grep -q "Fresh Green is RESERVED FOR AI OUTPUTS ONLY" app/globals.css &&
      npm run build > /tmp/02-03-t1-build.log 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - All 18 `--color-*` tokens present with exact hex values from ORIGIN_DESIGN.md §8.1: three trad-green variants, three fresh-green variants, six neutrals, three signals, three dark
    - All 9 `--spacing-*` tokens present (4, 8, 12, 16, 24, 32, 48, 64, 96 px)
    - All 3 `--radius-*` tokens present (button: 6px, card: 12px, modal: 16px)
    - All 4 `--font-*` remap entries inside `@theme inline {}` block (NOT inside the literal `@theme {}` block — D-33 chain requires `inline`)
    - `@import "tailwindcss";` is the first non-comment line (D-29)
    - Code comment near `--color-fresh-green` mentions "RESERVED FOR AI OUTPUTS ONLY" referencing CLAUDE.md (D-28; full enforcement deferred to Phase 4)
    - `body` reset uses `var(--color-paper)`, `var(--color-ink)`, `var(--font-body)` (D-29 minimal base reset)
    - `npm run build` exits 0 (PostCSS + Tailwind v4 compile the @theme blocks without errors)
  </acceptance_criteria>
  <done>app/globals.css ports the full ORIGIN_DESIGN §8 token set with two @theme blocks (literal + inline) plus minimal body reset; build succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire four next/font/google fonts in app/layout.tsx with .variable className composition</name>
  <files>app/layout.tsx</files>
  <read_first>
    - DECISIONS.md (D-30 next/font/google, D-31 Fraunces wght axis only, D-32 Noto Sans JP latin only, D-33 chain to @theme inline)
    - .planning/phases/02-scaffolding/02-RESEARCH.md (§5 "app/layout.tsx — four fonts via next/font/google" lines 351–420, §"Landmines" #3 next/font CSS vars)
    - .planning/phases/02-scaffolding/02-PATTERNS.md (§"app/layout.tsx", Pattern B "next/font/google → CSS variable → @theme inline → utility")
    - app/globals.css (just written in Task 1 — confirms which CSS var names the @theme inline block expects)
    - app/layout.tsx (current state — created by create-next-app in 02-01 with Geist setup)
  </read_first>
  <action>
    Replace the entire content of `app/layout.tsx` with the four-font setup below (verbatim from RESEARCH §5 lines 353–407). **Use the Write tool** (full rewrite of the create-next-app default file).

    The CSS variable names used here (`--font-fraunces`, `--font-inter-tight`, `--font-noto-sans-jp`, `--font-ibm-plex-mono`) MUST match exactly what `app/globals.css` `@theme inline {}` references (Task 1) — they are paired contracts.

    ```tsx
    import type { Metadata } from 'next'
    import { Fraunces, Inter_Tight, Noto_Sans_JP, IBM_Plex_Mono } from 'next/font/google'
    import './globals.css'

    // D-31: Fraunces — wght axis only (no opsz/SOFT/WONK in v1; deferred per CONTEXT §"Deferred Ideas")
    const fraunces = Fraunces({
      subsets: ['latin'],
      axes: ['wght'],
      variable: '--font-fraunces',
      display: 'swap',
    })

    // Inter Tight — variable font on Google Fonts; no weight specification needed.
    // Import name is `Inter_Tight` (underscore-separated, capitalized — next/font/google convention).
    const interTight = Inter_Tight({
      subsets: ['latin'],
      variable: '--font-inter-tight',
      display: 'swap',
    })

    // D-32: Noto Sans JP — subsets: ['latin'] only.
    // JP characters auto-load via Google Fonts unicode-range when they appear on the page.
    const notoSansJP = Noto_Sans_JP({
      subsets: ['latin'],
      variable: '--font-noto-sans-jp',
      display: 'swap',
    })

    // IBM Plex Mono — NOT a variable font on Google Fonts; weights must be explicit.
    // 400 (regular) for general use; 700 (bold) for ID / timestamp emphasis.
    const ibmPlexMono = IBM_Plex_Mono({
      subsets: ['latin'],
      weight: ['400', '700'],
      variable: '--font-ibm-plex-mono',
      display: 'swap',
    })

    export const metadata: Metadata = {
      title: 'SMBC Origin',
      description: 'AI-native corporate banking onboarding — prototype',
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html
          lang="en"
          className={`${fraunces.variable} ${interTight.variable} ${notoSansJP.variable} ${ibmPlexMono.variable}`}
        >
          <body>{children}</body>
        </html>
      )
    }
    ```

    **Critical contract details:**
    - The four `variable: '--font-{slug}'` strings here MUST match the four `var(--font-{slug})` references in `app/globals.css` `@theme inline {}` (Task 1)
    - All four `.variable` classNames on `<html>` (D-33) — next/font writes the CSS var on whatever element receives `.variable`; placing on `<html>` makes it global
    - `body` content is bare `<body>{children}</body>` — base styles (background, text color, default font) come from `globals.css` `body` reset

    **Capitalization note:** Next.js next/font/google uses `Inter_Tight` (with underscore, both words capitalized) for the multi-word "Inter Tight" font, `Noto_Sans_JP` for "Noto Sans JP", and `IBM_Plex_Mono` for "IBM Plex Mono". RESEARCH §5 line 416 verifies this convention.

    **Do NOT add** `axes: ['opsz']`, `axes: ['SOFT']`, or `axes: ['WONK']` to Fraunces — D-31 explicit exclusion (deferred per CONTEXT §"Deferred Ideas").
    **Do NOT add** `subsets: ['japanese']` to Noto Sans JP — D-32 mandates `latin` only with unicode-range autoload.
    **Do NOT** make IBM_Plex_Mono variable — RESEARCH §5 line 418 confirms it's NOT a variable font on Google Fonts.
  </action>
  <verify>
    <automated>
      cd /Users/wyekitgoh/Projects/SMBCorigins &&
      grep -q "from 'next/font/google'" app/layout.tsx &&
      grep -q "Fraunces" app/layout.tsx &&
      grep -q "Inter_Tight" app/layout.tsx &&
      grep -q "Noto_Sans_JP" app/layout.tsx &&
      grep -q "IBM_Plex_Mono" app/layout.tsx &&
      grep -q "axes: \['wght'\]" app/layout.tsx &&
      grep -q "weight: \['400', '700'\]" app/layout.tsx &&
      grep -q "variable: '--font-fraunces'" app/layout.tsx &&
      grep -q "variable: '--font-inter-tight'" app/layout.tsx &&
      grep -q "variable: '--font-noto-sans-jp'" app/layout.tsx &&
      grep -q "variable: '--font-ibm-plex-mono'" app/layout.tsx &&
      grep -q 'fraunces\.variable' app/layout.tsx &&
      grep -q 'interTight\.variable' app/layout.tsx &&
      grep -q 'notoSansJP\.variable' app/layout.tsx &&
      grep -q 'ibmPlexMono\.variable' app/layout.tsx &&
      ! grep -q "axes: \['opsz'\]" app/layout.tsx &&
      ! grep -q "axes: \['SOFT'\]" app/layout.tsx &&
      ! grep -q "axes: \['WONK'\]" app/layout.tsx &&
      ! grep -q "subsets: \['japanese'\]" app/layout.tsx &&
      grep -q "title: 'SMBC Origin'" app/layout.tsx &&
      npm run typecheck > /tmp/02-03-t2-typecheck.log 2>&1 &&
      npm run build > /tmp/02-03-t2-build.log 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - All four font import statements present from `next/font/google`
    - Fraunces config: `subsets: ['latin']`, `axes: ['wght']` (NOT opsz/SOFT/WONK), `variable: '--font-fraunces'`, `display: 'swap'`
    - Inter Tight config: variable font (no `weight`), `variable: '--font-inter-tight'`
    - Noto Sans JP config: `subsets: ['latin']` only (NOT `japanese`), `variable: '--font-noto-sans-jp'`
    - IBM Plex Mono config: `weight: ['400', '700']` (REQUIRED — not a variable font), `variable: '--font-ibm-plex-mono'`
    - All four `.variable` classNames composed onto `<html>` via template-literal concatenation
    - `metadata` block exports `{ title: 'SMBC Origin', description: 'AI-native corporate banking onboarding — prototype' }`
    - `<body>{children}</body>` is bare (no inline className — base styles in globals.css per Task 1)
    - The four `variable:` values here EXACTLY match the four `var(--font-...)` references in app/globals.css @theme inline (paired contract — verified by visual cross-check)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>app/layout.tsx loads all four Google Fonts via next/font/google, applies four .variable classNames to <html>, sets metadata. Paired with app/globals.css @theme inline remap from Task 1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build root token showcase page (D-41) — 18 swatches + 4 font samples + Japanese line + mono ID sample</name>
  <files>app/page.tsx</files>
  <read_first>
    - .planning/phases/02-scaffolding/02-CONTEXT.md (D-41 placeholder content — root showcase)
    - .planning/phases/02-scaffolding/02-RESEARCH.md (§"Validation Architecture" Wave 0 Gaps lines 740–750 — checklist a reviewer can scan)
    - .planning/phases/02-scaffolding/02-PATTERNS.md (§"app/page.tsx")
    - docs/ORIGIN_DESIGN.md §8.1 (canonical hex values to display in swatches)
    - app/globals.css (just written in Task 1 — confirms which utility class names the showcase can use)
    - app/layout.tsx (just written in Task 2 — confirms font utilities `font-display`, `font-body`, `font-jp`, `font-mono` resolve)
    - app/page.tsx (current state — created by create-next-app default; replace entirely)
  </read_first>
  <action>
    Replace the entire content of `app/page.tsx` with a token showcase page rendering a labeled swatch grid + four font samples. **Use the Write tool** (full rewrite of create-next-app default).

    The page MUST satisfy the D-41 + RESEARCH §"Wave 0 Gaps" verification checklist:
    1. 18 color swatches grouped by category, each labeled with token name + hex value
    2. Four labeled font sample paragraphs, each font's name set in itself
    3. Japanese sample line in `font-jp` to prove unicode-range autoload (e.g., 縁 / 海星製造株式会社 / ようこそ)
    4. IBM Plex Mono ID/timestamp sample (e.g., `RM-0001 / 2026-04-25T14:30:00Z`)
    5. Minimal layout: `bg-paper text-trad-green` (D-41 — no app shell; that's Phase 4)

    Use this implementation. The swatch arrays are typed; the rendering is flat JSX with no array-index access (avoids RESEARCH §"Landmines" #5 noUncheckedIndexedAccess pitfall):

    ```tsx
    // app/page.tsx
    // Phase 2 verification surface (D-41) — token showcase for SCAFF-04 + SCAFF-05.
    // Reviewers confirm Phase 2 done by opening this page and scanning swatches + fonts.
    // Phase 4 (app shell) supersedes this content; downstream phases create their own routes.

    type Swatch = {
      readonly token: string
      readonly hex: string
      readonly utilityBg: string
      readonly note?: string
    }

    type FontSample = {
      readonly name: string
      readonly utilityFont: string
      readonly sampleEn: string
      readonly sampleAlt?: string
    }

    // Brand greens — primary SMBC palette
    const brandGreens: ReadonlyArray<Swatch> = [
      { token: '--color-trad-green', hex: '#004832', utilityBg: 'bg-trad-green' },
      { token: '--color-trad-green-deep', hex: '#00301F', utilityBg: 'bg-trad-green-deep' },
      { token: '--color-trad-green-soft', hex: '#1A5F48', utilityBg: 'bg-trad-green-soft' },
      { token: '--color-fresh-green', hex: '#BFD730', utilityBg: 'bg-fresh-green', note: 'AI-only (D-28)' },
      { token: '--color-fresh-green-mute', hex: '#D4E566', utilityBg: 'bg-fresh-green-mute', note: 'AI-only (D-28)' },
      { token: '--color-fresh-green-glow', hex: '#BFD73022', utilityBg: 'bg-fresh-green-glow', note: 'AI-only (D-28)' },
    ]

    const neutrals: ReadonlyArray<Swatch> = [
      { token: '--color-ink', hex: '#0A1410', utilityBg: 'bg-ink' },
      { token: '--color-ink-soft', hex: '#3C4540', utilityBg: 'bg-ink-soft' },
      { token: '--color-ink-muted', hex: '#7A827D', utilityBg: 'bg-ink-muted' },
      { token: '--color-paper', hex: '#FAFBF7', utilityBg: 'bg-paper' },
      { token: '--color-paper-deep', hex: '#F3F5EE', utilityBg: 'bg-paper-deep' },
      { token: '--color-mist', hex: '#E8EDE4', utilityBg: 'bg-mist' },
    ]

    const signals: ReadonlyArray<Swatch> = [
      { token: '--color-signal-amber', hex: '#E8A317', utilityBg: 'bg-signal-amber' },
      { token: '--color-signal-red', hex: '#C73E1D', utilityBg: 'bg-signal-red' },
      { token: '--color-signal-info', hex: '#2A6F97', utilityBg: 'bg-signal-info' },
    ]

    const darkMode: ReadonlyArray<Swatch> = [
      { token: '--color-dark-bg', hex: '#0B1F17', utilityBg: 'bg-dark-bg' },
      { token: '--color-dark-surface', hex: '#112820', utilityBg: 'bg-dark-surface' },
      { token: '--color-dark-text', hex: '#E8EDE4', utilityBg: 'bg-dark-text' },
    ]

    const fontSamples: ReadonlyArray<FontSample> = [
      {
        name: 'Fraunces',
        utilityFont: 'font-display',
        sampleEn: 'Fraunces — display, headings, stat numerals',
      },
      {
        name: 'Inter Tight',
        utilityFont: 'font-body',
        sampleEn: 'Inter Tight — UI body. The quick brown fox jumps over the lazy dog.',
      },
      {
        name: 'Noto Sans JP',
        utilityFont: 'font-jp',
        sampleEn: 'Noto Sans JP — Japanese coverage',
        sampleAlt: '縁 · 海星製造株式会社 · Yukiさん、ようこそ',
      },
      {
        name: 'IBM Plex Mono',
        utilityFont: 'font-mono',
        sampleEn: 'IBM Plex Mono — data, IDs, timestamps',
        sampleAlt: 'RM-0001 / 2026-04-25T14:30:00Z',
      },
    ]

    function SwatchGrid({ title, items }: { title: string; items: ReadonlyArray<Swatch> }) {
      return (
        <section className="mb-12">
          <h2 className="font-display text-2xl mb-4">{title}</h2>
          <div className="grid grid-cols-3 gap-4">
            {items.map((swatch) => (
              <article key={swatch.token} className="rounded-card overflow-hidden border border-mist">
                <div className={`${swatch.utilityBg} h-24 w-full`} />
                <div className="p-3 bg-paper-deep">
                  <p className="font-mono text-sm text-ink">{swatch.token}</p>
                  <p className="font-mono text-xs text-ink-soft">{swatch.hex}</p>
                  {swatch.note ? (
                    <p className="font-mono text-xs text-signal-amber mt-1">{swatch.note}</p>
                  ) : null}
                </div>
              </article>
            ))}
          </div>
        </section>
      )
    }

    export default function TokenShowcase() {
      return (
        <main className="bg-paper text-trad-green min-h-screen p-12">
          <header className="mb-12">
            <h1 className="font-display text-5xl">SMBC Origin — Phase 2 Token Showcase</h1>
            <p className="font-body text-lg mt-2 text-ink-soft">
              Verification surface for SCAFF-04 (palette) and SCAFF-05 (typography). Source of truth: docs/ORIGIN_DESIGN.md §8.
            </p>
          </header>

          <SwatchGrid title="Brand greens" items={brandGreens} />
          <SwatchGrid title="Neutrals" items={neutrals} />
          <SwatchGrid title="Signals" items={signals} />
          <SwatchGrid title="Dark mode (RM cockpit option — tokens only, not wired in v1)" items={darkMode} />

          <section className="mb-12">
            <h2 className="font-display text-2xl mb-4">Typography</h2>
            <div className="grid grid-cols-1 gap-6">
              {fontSamples.map((font) => (
                <article key={font.name} className="border border-mist rounded-card p-6 bg-paper-deep">
                  <p className={`${font.utilityFont} text-3xl text-trad-green`}>{font.name}</p>
                  <p className={`${font.utilityFont} text-base text-ink mt-2`}>{font.sampleEn}</p>
                  {font.sampleAlt ? (
                    <p className={`${font.utilityFont} text-base text-ink mt-1`}>{font.sampleAlt}</p>
                  ) : null}
                </article>
              ))}
            </div>
          </section>

          <footer className="mt-16 pt-8 border-t border-mist">
            <p className="font-mono text-xs text-ink-muted">
              Phase 2 placeholder — Phase 4 (app shell) will supersede this root route.
            </p>
            <nav className="mt-4 flex gap-4">
              <a href="/journey" className="font-mono text-signal-info underline">→ /journey (client placeholder)</a>
              <a href="/cockpit" className="font-mono text-signal-info underline">→ /cockpit (RM placeholder)</a>
            </nav>
          </footer>
        </main>
      )
    }
    ```

    **Why `ReadonlyArray<Swatch>` and `.map()` instead of indexed access:** RESEARCH §"Landmines" #5 warns that `noUncheckedIndexedAccess: true` (D-05, Plan 02-02) flags every `arr[i]` as `T | undefined`. Using `.map()` returns a typed iteration without indexed access — typecheck stays clean.

    **Layout choices:**
    - 3-column grid for swatches — readable at 1440px desktop (PROJECT.md desktop-first principle)
    - `font-display` for headings (uses Fraunces via @theme inline mapping from Task 1)
    - `font-mono` for token names + hex values (legibility for data-shaped content; ORIGIN_DESIGN.md §8.2 — IBM Plex Mono is for data/IDs)
    - `bg-paper` page background, `text-trad-green` page text (D-41 + ORIGIN_DESIGN.md §8.1 brand pairing)
    - `border border-mist` and `rounded-card` (12px from Task 1's `--radius-card`) for swatch cards
    - Two footer nav links to `/journey` and `/cockpit` — these resolve in Plan 02-04; for now they will 404 if clicked. That is acceptable — the showcase page itself is the Phase 2 verification surface; cross-route navigation works after Plan 02-04 ships.

    **Note on the `<a href="/journey">` and `<a href="/cockpit">` links and `typedRoutes: true`:** With `typedRoutes` enabled (Plan 02-02 Task 2), Next.js generates types only for routes that exist at build time. Plan 02-04 adds `/journey` and `/cockpit`. If this plan (02-03) is built BEFORE 02-04 lands, the build may emit a typedRoutes warning for these hrefs. To avoid that warning while preserving the navigation intent, we use plain `<a>` (not `<Link>`) — `<a>` is not subject to typedRoutes. Phase 4 swaps these to `<Link>` once the routes are stable.
  </action>
  <verify>
    <automated>
      cd /Users/wyekitgoh/Projects/SMBCorigins &&
      grep -q "Token Showcase" app/page.tsx &&
      grep -q "bg-trad-green" app/page.tsx &&
      grep -q "bg-fresh-green" app/page.tsx &&
      grep -q "bg-paper" app/page.tsx &&
      grep -q "bg-mist" app/page.tsx &&
      grep -q "bg-signal-amber" app/page.tsx &&
      grep -q "bg-dark-bg" app/page.tsx &&
      grep -q "font-display" app/page.tsx &&
      grep -q "font-body" app/page.tsx &&
      grep -q "font-jp" app/page.tsx &&
      grep -q "font-mono" app/page.tsx &&
      grep -q "Fraunces" app/page.tsx &&
      grep -q "Inter Tight" app/page.tsx &&
      grep -q "Noto Sans JP" app/page.tsx &&
      grep -q "IBM Plex Mono" app/page.tsx &&
      grep -q "縁" app/page.tsx &&
      grep -q "RM-0001" app/page.tsx &&
      grep -q "rounded-card" app/page.tsx &&
      grep -q "ReadonlyArray" app/page.tsx &&
      ! grep -E "swatches?\[[0-9]+\]|fontSamples?\[[0-9]+\]" app/page.tsx &&
      npm run typecheck > /tmp/02-03-t3-typecheck.log 2>&1 &&
      npm run lint > /tmp/02-03-t3-lint.log 2>&1 &&
      npm run build > /tmp/02-03-t3-build.log 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - File contains "Token Showcase" heading text
    - File contains the six brand-green token names (`--color-trad-green`, `-deep`, `-soft`, `--color-fresh-green`, `-mute`, `-glow`) AND their hex values displayed
    - File contains the six neutral token names + hex values
    - File contains the three signal token names + hex values
    - File contains the three dark-mode token names + hex values (18 swatches total)
    - File uses utilities `bg-trad-green`, `bg-fresh-green`, `bg-paper`, `bg-mist`, `bg-signal-amber`, `bg-dark-bg` (proves Tailwind v4 generated utilities from Task 1's `@theme {}`)
    - File contains four font sample sections labeled "Fraunces", "Inter Tight", "Noto Sans JP", "IBM Plex Mono" — each set in its own font via `font-display`/`font-body`/`font-jp`/`font-mono`
    - File contains a Japanese sample (the character `縁` is present at minimum, plus 海星 / Yukiさん)
    - File contains an IBM Plex Mono ID/timestamp sample with `RM-0001`
    - File uses `rounded-card` (proves Task 1's radius tokens generate utilities)
    - File does NOT use indexed array access (`arr[0]`, `arr[i]`) — RESEARCH §"Landmines" #5 mitigation; uses `.map()` only
    - File uses `ReadonlyArray<T>` typing for swatch and font-sample collections
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - `npm run dev` (manual smoke — not in automated verify) renders the showcase at http://localhost:3000 with HTTP 200
  </acceptance_criteria>
  <done>app/page.tsx renders the D-41 token showcase: 18 labeled swatches in four groups + four font samples (each in its own face) + Japanese line + mono ID sample. Build, typecheck, lint all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Google Fonts CDN → Next.js build` | next/font/google fetches Google Fonts at build time (Vercel build pipeline) and self-hosts them. Build-time integrity depends on Google Fonts availability. |
| `app/page.tsx → browser DOM` | Static React server component output rendered to the user. No user input, no dynamic data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | Google Fonts CDN integrity | accept | next/font/google fetches Google Fonts at build time and self-hosts; runtime requests stay on Vercel's CDN, not Google's. Even if Google Fonts CDN were compromised in the build window, the self-hosted output is fixed at build. Stakeholder demo prototype risk profile permits accept. |
| T-02-11 | Information Disclosure | Token hex values in `app/page.tsx` | accept | Color hex values are public design tokens, not secrets. Displaying them on the showcase is the explicit purpose (D-41 — "reviewing the page IS the test"). No exfiltration risk. |
| T-02-12 | Denial of Service | next/font/google build-time fetch | accept | Build fails if Google Fonts CDN is unreachable at build time. Vercel's build pipeline retries automatically; if it persists, the build error is a clear DoS signal not a silent failure. Demo prototype scope permits accept. |
| T-02-13 | Tampering | `app/globals.css` token literal drift | mitigate | Task 1's automated grep checks each of the 18 hex values explicitly. Any future PR that mutates a hex value will fail Task 1's `<verify>` block (when re-run via lint hooks or CI in Plan 02-05). |

All threats LOW severity for a stakeholder demo prototype. No HIGH-severity threats introduced.
</threat_model>

<verification>
**Plan-level verification (run after all tasks complete):**

```bash
cd /Users/wyekitgoh/Projects/SMBCorigins
npm run typecheck && npm run lint && npm run build
```
All three commands MUST exit 0.

**File presence + content checks:**
- `grep -c "^\s*--color-" app/globals.css` ≥ 18 (covers all color tokens)
- `grep -c "^\s*--spacing-" app/globals.css` ≥ 9
- `grep -c "^\s*--radius-" app/globals.css` ≥ 3
- `grep -c "^\s*--font-" app/globals.css` ≥ 4 (in @theme inline)
- `grep -c "from 'next/font/google'" app/layout.tsx` = 1 (single import line, four named imports)
- `grep -c "fontSamples" app/page.tsx` ≥ 1 (showcase data)

**Manual visual verification (run `npm run dev` and open http://localhost:3000):**
1. Page loads with warm paper background (`bg-paper` = #FAFBF7) and Trad Green text (`text-trad-green` = #004832)
2. Four swatch grids visible: Brand greens (6), Neutrals (6), Signals (3), Dark mode (3) = 18 total
3. Each swatch has a colored block + the token name in `font-mono` + the hex value in `font-mono`
4. Fresh-green swatches (3) have an additional "AI-only (D-28)" note in `font-mono text-signal-amber`
5. Four font sample articles: each font's name set in its own face (Fraunces in serif, Inter Tight in sans, Noto Sans JP in JP-serif-ish, IBM Plex Mono in monospace)
6. Noto Sans JP article includes the Japanese line (`縁 · 海星製造株式会社 · Yukiさん、ようこそ`) — confirms unicode-range autoload working
7. IBM Plex Mono article includes the timestamp sample (`RM-0001 / 2026-04-25T14:30:00Z`)
8. DevTools "Computed" tab on a `bg-trad-green` element shows `background-color: rgb(0, 72, 50)` (= #004832 — RESEARCH §"Phase Requirements → Validation Map" SCAFF-03)
9. DevTools "Computed" tab on the `font-display` text shows `font-family: __Fraunces_xxx, ...` (proves next/font is wired)

These manual checks satisfy SCAFF-03 (utility classes apply), SCAFF-04 (palette renders), SCAFF-05 (four fonts visibly render).
</verification>

<success_criteria>
- All 18 color tokens from ORIGIN_DESIGN.md §8.1 present verbatim in app/globals.css `@theme {}` (SCAFF-04 — D-27 mandate)
- All 9 spacing + 3 radius tokens present
- `@theme inline {}` block remaps four next/font CSS vars to Tailwind font namespace (D-33; RESEARCH §"Landmines" #3 — `inline` keyword required)
- All four fonts loaded via next/font/google with correct config: Fraunces wght axis only (D-31), Noto Sans JP latin only (D-32), IBM Plex Mono explicit weights (RESEARCH §5)
- All four `.variable` classNames composed onto `<html>` (D-33)
- Root `/` page renders 18 swatches + 4 font samples + Japanese line + mono ID sample (D-41 verification surface)
- `npm run typecheck && npm run lint && npm run build` all green
- Manual: page renders correctly at http://localhost:3000 with all swatches and fonts visible (SCAFF-03 utilities, SCAFF-04 palette, SCAFF-05 fonts)
</success_criteria>

<output>
After completion, create `.planning/phases/02-scaffolding/02-03-SUMMARY.md` documenting:
- Final list of all 18 color tokens with hex values (cross-referenced to ORIGIN_DESIGN.md §8.1)
- Final list of all 9 spacing + 3 radius tokens
- Font config summary table: Font / subsets / axes-or-weight / CSS var name
- Confirmation that the four CSS var names in app/layout.tsx match the four var(--font-...) references in app/globals.css
- Screenshot or descriptive note of the rendered showcase page (D-41 verification surface)
- Build output (succeeded under what next.js / tailwindcss versions)
</output>
