---
phase: 01-setup-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRACT.md
autonomous: true
requirements:
  - SETUP-01
tags:
  - governance
  - boundary-contract
  - phase-1
must_haves:
  truths:
    - "CONTRACT.md exists at repo root and governs the cross-GSD boundary"
    - "CONTRACT.md describes ownership rules for types/origin.ts, lib/api.ts, lib/api.mock.ts, lib/api.real.ts"
    - "CONTRACT.md encodes first-PR-wins conflict resolution and Slack-ping convention"
    - "CONTRACT.md is human-readable governance — backed by .github/CODEOWNERS for enforcement"
  artifacts:
    - path: "CONTRACT.md"
      provides: "Boundary governance for the cross-GSD shared interface"
      min_lines: 40
      contains: "first-PR-wins"
  key_links:
    - from: "CONTRACT.md"
      to: ".github/CODEOWNERS"
      via: "CONTRACT.md is the human prose; CODEOWNERS is the GitHub enforcement"
      pattern: "CODEOWNERS"
    - from: "CONTRACT.md"
      to: "DECISIONS.md (D-13..D-16, D-23)"
      via: "CONTRACT.md operationalizes the cross-GSD sync mechanism decisions"
      pattern: "types/origin.ts"
---

<objective>
Author `CONTRACT.md` at the repository root — a human-readable governance document for the cross-GSD shared boundary. It describes co-ownership rules for `types/origin.ts`, the split `lib/api.ts` / `lib/api.mock.ts` / `lib/api.real.ts` family (per D-16), the conflict resolution protocol ("first-PR-wins; second rebases" per D-15), and the Slack-ping-on-contract-PR convention (per D-14).

Purpose: Phase 3 (Shared Boundary) creates `types/origin.ts`, `lib/api.ts`, `lib/api.mock.ts`, `lib/api.real.ts`. CONTRACT.md pre-establishes the rules so the moment those files exist, governance is already documented and Evan can reason about the boundary without learning GSD-specific tooling (D-21).

Output: `CONTRACT.md` at the repo root, structured into named sections covering the boundary files, ownership matrix, conflict resolution, and announcement convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-setup-alignment/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRACT.md at repo root with boundary governance prose</name>
  <files>CONTRACT.md</files>
  <read_first>
    - .planning/phases/01-setup-alignment/01-CONTEXT.md (CANONICAL — D-13 through D-16, D-23, D-24 specify the contract)
    - .planning/REQUIREMENTS.md (§ "Shared Boundary (BOUND)" — BOUND-01..04 list the four file paths CONTRACT.md governs)
    - CLAUDE.md (current "Working principles" section already says "Never edit `types/origin.ts` without telling the other person" — CONTRACT.md formalizes this)
    - CONTRACT.md (read-attempt to confirm file does NOT yet exist; this task creates it from scratch)
  </read_first>
  <action>
Create a new file `CONTRACT.md` at the repository root (NOT inside `.planning/` — D-21 forbids that).

Write the file with EXACTLY this content (copy verbatim):

```markdown
# CONTRACT — Cross-GSD Shared Boundary Governance

This document governs the typed boundary between Kit's frontend GSD and Evan's backend GSD. It describes ownership, change protocol, and conflict resolution for the four shared boundary files.

The active locks are recorded in `DECISIONS.md` (canonical, append-only). On conflict between this document and `DECISIONS.md`, `DECISIONS.md` wins. This file exists for human readability and onboarding — Evan does not use GSD or `.planning/` (D-21), so all governance is documented here at the repo root.

## Boundary files

The four files below form the cross-GSD shared boundary. They will be created in Phase 3 (Shared Boundary, requirements `BOUND-01..04`); this contract pre-establishes their governance so it is in force the moment those files first appear.

| File | Role | Owner | Reviewer |
|------|------|-------|----------|
| `types/origin.ts` | Single source of truth for shared types: `Application`, `Entity`, `UBO`, `Document`, `ScreeningHit`, `CreditMemo`, `Stage`, `User` | Co-owned (Kit + Evan) | The other person — auto-requested via `.github/CODEOWNERS` |
| `lib/api.ts` | Typed top-level API export. Switches between mock and real backends on `NEXT_PUBLIC_USE_MOCK`. Contains no business logic — only the switch and the type-level export surface. | Co-owned (Kit + Evan) | The other person — auto-requested via `.github/CODEOWNERS` |
| `lib/api.mock.ts` | Mock implementation reading from `data/seed.ts`. The default path when `NEXT_PUBLIC_USE_MOCK=true`. | Kit | Evan reviews on PR (auto-requested via `.github/CODEOWNERS`) |
| `lib/api.real.ts` | Real implementation reading from Evan's Supabase + API routes. The path when `NEXT_PUBLIC_USE_MOCK=false`. | Evan | Kit reviews on PR (auto-requested via `.github/CODEOWNERS`) |

Splitting `lib/api.ts` into `lib/api.mock.ts` + `lib/api.real.ts` (with `lib/api.ts` as the typed switch) gives clean file-level ownership for the implementation halves while keeping the public surface co-owned. This still satisfies `BOUND-02` because `lib/api.ts` remains the typed client export — it just delegates internally based on the env flag.

## Co-ownership rules

1. **Cross-review required on every touching PR.** GitHub auto-requests the other person as reviewer via `.github/CODEOWNERS`. Do not bypass.
2. **Never edit `types/origin.ts` without telling the other person.** This is a working principle from `CLAUDE.md` and is now contract-level.
3. **Type changes are breaking changes by default.** Treat any addition, removal, or rename in `types/origin.ts` as breaking. The author owns the migration on their side; the reviewer owns the migration on theirs.
4. **The two halves of `lib/api.*` must stay shape-compatible.** Both `lib/api.mock.ts` and `lib/api.real.ts` must export the same function signatures (typed against `types/origin.ts`). The top-level `lib/api.ts` switch is the compile-time gate.

## Conflict resolution: first-PR-wins

When two PRs touch the same boundary file (e.g. both add a field to `types/origin.ts`):

1. **First PR to merge wins.** Whoever merges first sets the new shape.
2. **The second PR rebases on `main`** and resolves the conflict locally — re-running typecheck to confirm the merged shape still works for their side.
3. **The second PR re-requests review** after rebase. The reviewer re-approves only after confirming the rebased diff still matches the original intent.
4. **No special tooling.** This is standard git flow; no merge queue, no priority claims, no out-of-band locking.

## Announcement convention: Slack-ping on contract PRs

Any PR that touches `types/origin.ts` or `lib/api.ts` (the co-owned files):

1. **Author pings the shared Slack channel** with the PR link the moment they open it.
2. **Body of the ping** must include: a one-line summary of the change, and a callout of any breaking type changes.
3. **The reviewer commits to a turnaround target** in the channel (or replies "blocked, will review at <time>").
4. **No polling.** GitHub's auto-request via CODEOWNERS plus the Slack ping is the full notification path. Don't wait silently for review.

## Branch protection

Per `D-08` (recorded in `DECISIONS.md`): `main` is PR-protected. Status checks (CI typecheck + lint + Vercel preview) must pass before merge. No required reviewers at the branch-protection level — required reviewers come from `.github/CODEOWNERS` for the four boundary files only.

## Soft-block on Phase 2 merge

Per `D-19`: if Evan cannot review the Phase 1 closure PR (`kit/phase-1-alignment`) in the target window, Kit may begin Phase 2 work on a separate branch (`kit/scaffold`) using these locks but cannot merge to `main` until Phase 1 closes. This preserves the day-1 deploy schedule without front-running Evan's sign-off.

## Updating this contract

This file is governance, not code. Updates follow the same flow as decisions:

1. Record the new decision in `DECISIONS.md` first (append-only line; signed).
2. Edit `CONTRACT.md` to reflect the new state.
3. Open a single PR with both changes; both Kit and Evan approve before merge.

If `CONTRACT.md` and `DECISIONS.md` ever disagree, `DECISIONS.md` wins (per `D-20`) and `CONTRACT.md` is corrected.
```

Use the Write tool, not heredoc/cat. The em-dash characters are U+2014 (`—`).
  </action>
  <verify>
    <automated>test -f CONTRACT.md && grep -E "^## " CONTRACT.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f CONTRACT.md` exits 0
    - At least 6 H2 sections: `test "$(grep -cE '^## ' CONTRACT.md)" -ge 6` exits 0
    - All four boundary files referenced: `grep -q "types/origin.ts" CONTRACT.md && grep -q "lib/api.ts" CONTRACT.md && grep -q "lib/api.mock.ts" CONTRACT.md && grep -q "lib/api.real.ts" CONTRACT.md` exits 0
    - First-PR-wins rule documented: `grep -q "first-PR-wins" CONTRACT.md || grep -q "First PR to merge wins" CONTRACT.md` exits 0
    - Slack-ping convention documented: `grep -qi "slack" CONTRACT.md` exits 0
    - CODEOWNERS reference present: `grep -q "CODEOWNERS" CONTRACT.md` exits 0
    - Canonical-source disclaimer present: `grep -q "DECISIONS.md wins" CONTRACT.md` exits 0
    - Owner attribution clear: `grep -q "| Kit |" CONTRACT.md && grep -q "| Evan |" CONTRACT.md` exits 0
    - File lives at repo root, NOT in `.planning/`: `test ! -f .planning/CONTRACT.md` exits 0
  </acceptance_criteria>
  <done>
    CONTRACT.md exists at repo root with all required sections, ready to be included in the Phase 1 closure PR (D-17).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub remote | CONTRACT.md is committed and pushed; integrity depends on branch protection (D-08) and PR review (D-17, D-18) |
| Cross-GSD (Kit ↔ Evan) | CONTRACT.md is the human-readable interface between two independent toolchains; ambiguity here causes future contract drift |

## STRIDE Threat Register

This phase ships docs only — no executable code, no runtime input, no network listeners, no data store. The threat surface is repository-governance integrity.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CONTRACT.md silently rewritten without DECISIONS.md update | mitigate | D-20 makes DECISIONS.md canonical and the "Updating this contract" section in CONTRACT.md enforces the dual-write flow. Branch protection (D-08) ensures any change requires PR review |
| T-02-02 | Repudiation | "We never agreed to first-PR-wins" disputed later | mitigate | D-15 is recorded as a signed line in DECISIONS.md (Plan 01); CONTRACT.md is the operational expansion of that decision. Both files ship in the same closure PR, so PR approval covers both |
| T-02-03 | Information Disclosure | Contract reveals internal team norms | accept | Repo is intentionally semi-public; team norms (Slack, daily-merge) are not sensitive |
| T-02-04 | Elevation of Privilege | Contributor edits CONTRACT.md to weaken governance (e.g. removes cross-review requirement) | mitigate | Plan 03 (CODEOWNERS) does NOT auto-request review on `CONTRACT.md` itself — branch protection (D-08) is the only gate. This is intentional: governance docs need human review by maintainers, not file-pattern auto-routing. Both Kit and Evan are repo maintainers |
| T-02-05 | Spoofing / Denial of Service | n/a | n/a | Static markdown; no runtime |
</threat_model>

<verification>
- CONTRACT.md created at repo root with all required sections
- Boundary files (`types/origin.ts`, `lib/api.ts`, `lib/api.mock.ts`, `lib/api.real.ts`) named and assigned ownership
- First-PR-wins, Slack-ping, and CODEOWNERS references all present
- Canonical-source disclaimer (DECISIONS.md wins) present per D-20
- File NOT in `.planning/` (D-21 enforced)
</verification>

<success_criteria>
- `test -f CONTRACT.md` exits 0
- All four boundary files referenced; ownership matrix readable
- ROADMAP §Phase 1 Success Criterion #1 supported (cross-GSD stack contract documentation)
- ROADMAP §Phase 1 Success Criterion #4 (no code) — confirmed: this plan only adds Markdown
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-alignment/01-02-SUMMARY.md`
</output>
