---
phase: 01-setup-alignment
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
tags:
  - claude-md
  - stack-contract
  - scaffolding-ownership
  - phase-1
must_haves:
  truths:
    - "CLAUDE.md retains ALL existing sections unmodified except the placeholder Stack section"
    - "CLAUDE.md gains a new '## Stack contract' section summarizing D-01..D-08"
    - "CLAUDE.md gains a new '## Scaffolding ownership' section summarizing D-09..D-12"
    - "Both new sections explicitly point to DECISIONS.md as canonical source per D-20"
    - "Future Claude sessions reading CLAUDE.md immediately see the active locks"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Session-start brief now contains the locked stack and scaffolding decisions"
      contains: "## Stack contract"
  key_links:
    - from: "CLAUDE.md (Stack contract section)"
      to: "DECISIONS.md (D-01..D-08)"
      via: "CLAUDE.md summarizes; DECISIONS.md is canonical (D-20)"
      pattern: "DECISIONS.md"
    - from: "CLAUDE.md (Scaffolding ownership section)"
      to: "DECISIONS.md (D-09..D-12)"
      via: "CLAUDE.md summarizes; DECISIONS.md is canonical (D-20)"
      pattern: "DECISIONS.md"
---

<objective>
Mutate `CLAUDE.md` (existing repo-root file) to add two new sections: "Stack contract" (summarizes D-01..D-08) and "Scaffolding ownership" (summarizes D-09..D-12). The existing placeholder "Stack (to be locked together)" section is now obsolete and gets replaced; everything else in CLAUDE.md stays intact.

Purpose: CLAUDE.md is read by Claude Code at every session start. Per D-25, the new sections make the active locks visible to every future session — without requiring anyone to surface them — while DECISIONS.md remains canonical.

Output: `CLAUDE.md` updated in place, with two new H2 sections inserted under existing structure. Existing content above and below them unchanged.

This plan depends on Plan 01 (DECISIONS.md must exist as the canonical source CLAUDE.md points to).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-setup-alignment/01-CONTEXT.md
@.planning/phases/01-setup-alignment/01-01-SUMMARY.md
@CLAUDE.md
@DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Stack placeholder with Stack contract section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (CRITICAL — read full current state to know exactly what to replace and what to leave alone)
    - .planning/phases/01-setup-alignment/01-CONTEXT.md (CANONICAL — D-01..D-08 source values; section "Stack contract" decisions)
    - DECISIONS.md (created by Plan 01 — confirm D-01..D-08 lines exist; CLAUDE.md points to this file as canonical)
    - .planning/ROADMAP.md (section "Phase 1: Setup & Alignment" Success Criterion #1)
  </read_first>
  <action>
The current `CLAUDE.md` has a section titled "Stack (to be locked together)" with this content (4 bullets describing the family-level stack). REPLACE that entire section (heading + bullets) with a new "Stack contract" section.

Use the Edit tool. The old_string is the full existing placeholder section, exactly:

```
## Stack (to be locked together)

- Next.js 14+ (App Router) · TypeScript · Tailwind
- Supabase (Postgres, Auth, RLS)
- Anthropic Claude API for the 4 AI hero moments
- Deployed to Vercel from `main`
```

The new_string is EXACTLY:

```
## Stack contract

The locked technical stack agreed by Kit and Evan in Phase 1 (see `DECISIONS.md` D-01..D-08 for the canonical record — this is a summary).

- **Framework**: Next.js 16.2 LTS, App Router (D-01)
- **Runtime**: Node 24 LTS — pinned via `.nvmrc` and `engines.node` in `package.json` (D-02)
- **Styling**: Tailwind v4 with CSS-first `@theme` config (D-03)
- **Backend**: Supabase hosted dev project — Vercel previews flip `NEXT_PUBLIC_USE_MOCK=false` against a real backend (D-04)
- **TypeScript**: `strict: true` and `noUncheckedIndexedAccess: true` — `exactOptionalPropertyTypes` deliberately not adopted for prototype velocity (D-05)
- **Package manager**: npm (D-06)
- **Linter / formatter**: ESLint flat config + Prettier (D-07)
- **Branch protection on `main`**: PR required + status checks (CI typecheck + lint + Vercel preview); no required reviewers at branch level — cross-GSD review is enforced via `.github/CODEOWNERS` for the four boundary files only (D-08)
- **Deployment**: Vercel auto-deploy from `main`; PR previews per branch
- **Anthropic Claude API** for the four AI hero moments (Evan's GSD owns the integration)

On any conflict between this section and `DECISIONS.md`, `DECISIONS.md` wins (per D-20). Update both atomically when locks change.
```

Do NOT modify any other line in `CLAUDE.md`. The header, "What we're building", "Who's building it", "Working principles", "Personas", "Hero scenario", "The six stages", "Design system", "Reference docs", "Non-goals", and "How to run" sections all stay exactly as they are.
  </action>
  <verify>
    <automated>grep -cE "^## Stack contract$" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - New heading exists exactly once: `grep -cE "^## Stack contract$" CLAUDE.md` returns `1`
    - Old placeholder heading gone: `grep -cE "^## Stack \(to be locked together\)$" CLAUDE.md` returns `0`
    - All 8 stack decisions referenced inline: `for d in D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08; do grep -q "($d)" CLAUDE.md || echo "MISSING $d"; done` produces no output
    - Specific values present: each of `Next.js 16.2 LTS`, `Node 24 LTS`, `Tailwind v4`, `Supabase hosted dev project`, `noUncheckedIndexedAccess`, `ESLint flat config`, `npm` (in stack section), `Vercel auto-deploy` is found via `grep`
    - Canonical-source pointer present: `grep -q "DECISIONS.md" CLAUDE.md` exits 0
    - DECISIONS.md wins disclaimer present: `grep -q "DECISIONS.md wins" CLAUDE.md` exits 0
    - Other sections preserved (each grep below exits 0):
      - `grep -qE "^## What we're building$" CLAUDE.md`
      - `grep -qE "^## Who's building it$" CLAUDE.md`
      - `grep -qE "^## Working principles$" CLAUDE.md`
      - `grep -qE "^## Personas$" CLAUDE.md`
      - `grep -qE "^## Hero scenario$" CLAUDE.md`
      - `grep -qE "^## The six stages$" CLAUDE.md`
      - `grep -qE "^## Reference docs$" CLAUDE.md`
      - `grep -qE "^## Non-goals$" CLAUDE.md`
      - `grep -qE "^## How to run$" CLAUDE.md`
  </acceptance_criteria>
  <done>
    CLAUDE.md "Stack" placeholder section replaced with "Stack contract" section summarizing D-01..D-08. All other sections preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Scaffolding ownership section after Stack contract</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (CRITICAL — re-read after Task 1 to confirm "Stack contract" section is in place; identify the right insertion point)
    - .planning/phases/01-setup-alignment/01-CONTEXT.md (CANONICAL — D-09..D-12 source values; section "Scaffolding ownership & layout" decisions)
    - DECISIONS.md (Plan 01's output — confirm D-09..D-12 lines exist)
  </read_first>
  <action>
After Task 1, `CLAUDE.md` has a `## Stack contract` section. Insert a new H2 section `## Scaffolding ownership` IMMEDIATELY AFTER the `## Stack contract` section's closing paragraph (the "On any conflict... DECISIONS.md wins (per D-20)..." line) and BEFORE the next existing section (which is `## Working principles`).

Use the Edit tool. Find the boundary by matching `On any conflict between this section and \`DECISIONS.md\`, \`DECISIONS.md\` wins (per D-20). Update both atomically when locks change.` followed by a blank line followed by `## Working principles`. Replace that boundary with the closing line, a blank line, the new section in full, a blank line, then `## Working principles`.

The new section content is EXACTLY:

```
## Scaffolding ownership

Who owns initial repo scaffolding (Phase 2) and the directory layout that Phase 3+ depends on. Canonical record lives in `DECISIONS.md` D-09..D-12.

- **Initial scaffold author**: Kit alone runs `create-next-app` and pushes the initial scaffold; Evan reviews. This preserves the day-1 Vercel URL target. (D-09)
- **Directory layout**: root-level `app/`, `components/`, `lib/`, `types/`, `data/` — no `src/` wrapper. Matches `REQUIREMENTS.md` paths verbatim (BOUND-01..04, SCAFF-06). (D-10)
- **`types/origin.ts` at repo root** — single source of truth, max visibility, matches BOUND-01 path exactly. (D-11)
- **Initial-scaffold PR**: branch `kit/scaffold`, single PR, reviewed and merged by Evan. (D-12)

The four shared boundary files (`types/origin.ts`, `lib/api.ts`, `lib/api.mock.ts`, `lib/api.real.ts`) are governed by `CONTRACT.md` and enforced via `.github/CODEOWNERS` — see those files for the cross-review rules.
```

Do NOT modify any line outside this insertion point.
  </action>
  <verify>
    <automated>grep -cE "^## Scaffolding ownership$" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Heading exists exactly once: `grep -cE "^## Scaffolding ownership$" CLAUDE.md` returns `1`
    - All 4 scaffolding decisions referenced: `for d in D-09 D-10 D-11 D-12; do grep -q "($d)" CLAUDE.md || echo "MISSING $d"; done` produces no output
    - Specific values present: each of `kit/scaffold`, `create-next-app`, `types/origin.ts`, `BOUND-01`, `SCAFF-06` is found via `grep`
    - Pointer to CONTRACT.md present: `grep -q "CONTRACT.md" CLAUDE.md` exits 0
    - Pointer to .github/CODEOWNERS present: `grep -q ".github/CODEOWNERS" CLAUDE.md` exits 0
    - Stack contract section still exists: `grep -cE "^## Stack contract$" CLAUDE.md` returns `1`
    - All other earlier sections still preserved (rerun the section grep set from Task 1)
    - Section ordering correct: Stack contract appears before Scaffolding ownership before Working principles. Verify with `awk '/^## Stack contract$/{a=NR} /^## Scaffolding ownership$/{b=NR} /^## Working principles$/{c=NR} END{exit !(a<b && b<c)}' CLAUDE.md`
  </acceptance_criteria>
  <done>
    CLAUDE.md gains a "Scaffolding ownership" section between "Stack contract" and "Working principles", summarizing D-09..D-12. SETUP-01 + SETUP-02 are now satisfied at the CLAUDE.md level.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub remote | CLAUDE.md edits are committed and pushed; integrity depends on branch protection (D-08) and PR review (D-17, D-18) |
| CLAUDE.md ↔ DECISIONS.md | Two-document drift risk: edits to one without the other create canonical-source ambiguity |

## STRIDE Threat Register

This phase ships docs only — no executable code. The threat surface is canonical-source coherence between CLAUDE.md and DECISIONS.md.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | CLAUDE.md updated without DECISIONS.md update (drift) | mitigate | New "Stack contract" section explicitly says "On conflict, DECISIONS.md wins (per D-20). Update both atomically." This is the documented escape valve when drift happens — DECISIONS.md is unambiguous |
| T-04-02 | Tampering | Editor accidentally removes unrelated existing CLAUDE.md sections during the mutation | mitigate | Acceptance criteria for both tasks include a checklist of all surviving H2 headings; CI grep would catch a regression. Edit tool (vs Write) preserves surrounding content by design |
| T-04-03 | Repudiation | "We never agreed to Tailwind v4" disputed later | mitigate | Inline `(D-XX)` references in every bullet route disputes back to the signed line in DECISIONS.md (Plan 01) |
| T-04-04 | Information Disclosure | n/a | n/a | CLAUDE.md is already in the repo; this plan changes content but not exposure |
| T-04-05 | Spoofing / Elevation of Privilege | Bypass branch protection to push CLAUDE.md edits without review | mitigate | D-08 branch protection + PR-required gate applies. CLAUDE.md is not in CODEOWNERS by design (Plan 03 explicitly scopes CODEOWNERS to the 4 boundary files only); branch protection alone is sufficient for this docs-only file |
| T-04-06 | Denial of Service | n/a | n/a | Static markdown; no runtime |
</threat_model>

<verification>
- CLAUDE.md preserves all original sections except the replaced Stack placeholder
- New "Stack contract" section summarizes D-01..D-08 with inline references
- New "Scaffolding ownership" section summarizes D-09..D-12 with inline references
- Both new sections explicitly defer to DECISIONS.md as canonical (per D-20)
- ROADMAP §Phase 1 Success Criterion #1 (CLAUDE.md "Stack contract" section) — satisfied
- ROADMAP §Phase 1 Success Criterion #2 (CLAUDE.md "Scaffolding ownership" section) — satisfied
</verification>

<success_criteria>
- `grep -E "^## Stack contract$" CLAUDE.md` exits 0
- `grep -E "^## Scaffolding ownership$" CLAUDE.md` exits 0
- `grep -q "Next.js 16.2 LTS" CLAUDE.md` exits 0
- `grep -q "Node 24 LTS" CLAUDE.md` exits 0
- `grep -q "Tailwind v4" CLAUDE.md` exits 0
- `grep -q "kit/scaffold" CLAUDE.md` exits 0
- `grep -q "DECISIONS.md wins" CLAUDE.md` exits 0
- ROADMAP §Phase 1 Success Criterion #4 (no code) — confirmed: this plan only edits Markdown
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-alignment/01-04-SUMMARY.md`
</output>
